[project]
name = "ccpet"
version = "0.1.0"
description = "Claude Code 사용량 파서 + 검증 하네스"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["ccpet"]
